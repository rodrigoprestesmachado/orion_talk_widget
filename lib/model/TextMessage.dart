library orion_talk_widget;

/// # TextMessage class
///
/// This is a model class to encapsulate a message in text format
///
class TextMessage {
  String id;
  String text;

  TextMessage(this.id, this.text);
}
